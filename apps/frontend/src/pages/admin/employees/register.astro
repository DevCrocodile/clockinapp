---
import Layout from "@/layout/layout.astro";
import { RegisterEmployee } from "@/components/admin/employees/RegisterEmployee";
---

<Layout>
    <RegisterEmployee
        roles={[]}
        branches={[]}
        onBack={() => {}}
        onSave={() => {}}
        client:load
    />
</Layout>
