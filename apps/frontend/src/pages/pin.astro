---
import { PinLoginView } from "@/components/pinLogin/PinLoginView";
import Layout from "@/layout/layout.astro";
---

<Layout>
    <PinLoginView onPinSubmit={() => {}} onBack={() => {}} client:load/>
</Layout>
